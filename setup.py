from setuptools import setup

setup(
   name='archcode',
   version='1.0',
   description='a module for visualizing the structure of the neuromix module',
   author='Krubeal Danieli',
   author_email='dkirubel@gmail.com',
   packages=['archcode'],  #same as name
   install_requires=[], #external packages as dependencies
)
